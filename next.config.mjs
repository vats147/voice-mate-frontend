/** @type {import('next').NextConfig} */
const nextConfig = {};
module.exports = {
    experimental: {
      appDir: true, // Indicates App Router is enabled
    },
  };
  
export default nextConfig;
